pipeline {
    agent any
    stages {
        stage('corpdmz.staples.com') {
            steps {
                echo "copying from corpdmz.staples.com"
                withCredentials([usernamePassword(credentialsId:'958c6522-7c0d-4a59-bd03-15a6250a5d9f',usernameVariable:'USERNAME',passwordVariable:'PASSWORD')]){
                sh 'sftp ${USERNAME}@localhost:/tmp/test1.tar ${WORKSPACE}/test2.tar'
            }
            }
        }    
    }
}
